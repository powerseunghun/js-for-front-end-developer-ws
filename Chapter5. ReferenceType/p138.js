var colors = ["red", "blue", "green"];
alert(colors.toString());  // red,blue,green
alert(colors.valueOf());   // red,blue,green
alert(colors);             // red,blue,green
// alert()는 매개변수로 문자열을 받으므로 배열을 넘기게 되면 toString()을 호출